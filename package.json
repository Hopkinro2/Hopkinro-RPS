{
  "name": "game-setup-rps",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Game Setup Lobby + Rock Paper Scissors (DOM, Forms, Storage) — starter with Jest + Playwright tests.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5173",
    "test": "vitest",
    "test:e2e": "playwright test",
    "test:all": "npm run test && npm run build && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.0",
    "jsdom": "^26.1.0",
    "vite": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
